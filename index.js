$(document).ready(function() {
  $("#jobForm").submit(function(e) {
    e.preventDefault();
    $("#successMessage").removeClass("d-none");
    $(this).trigger("reset");
  });
});
  //  for web dev
  $(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();
    
    // Show success message
    $('#successMsg').removeClass('d-none');
    
    // Clear form
    this.reset();
  });
});
// for data & analatics
$(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();

    // Show success message
    $('#successMessage').removeClass('d-none');

    // Reset form fields
    this.reset();
  });
});

//for AI

$(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();

    // Show success message
    $('#successMessage').removeClass('d-none');

    // Clear form fields
    this.reset();
  });
});

// network engineer

$(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();

    // Show success alert
    $('#successMessage').removeClass('d-none');

    // Reset form fields
    this.reset();
  });
});

//it infracture

$(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();

    // Show success message
    $('#successMessage').removeClass('d-none');

    // Reset form
    this.reset();
  });
});
// seo speslist
$(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();

    // Show success message
    $('#successMessage').removeClass('d-none');

    // Reset the form
    this.reset();
  });
});
//contant marketing
$(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();

    // Show success message
    $('#successMessage').removeClass('d-none');

    // Reset the form fields
    this.reset();
  });
});
//social media
$(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();

    // Show success message
    $('#successMessage').removeClass('d-none');

    // Reset the form
    this.reset();
  });
});
//email marketing specilist
$(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();

    // Show success message
    $('#successMessage').removeClass('d-none');

    // Reset the form
    this.reset();
  });
});
// pcc specilist
$(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();

    // Show success message
    $('#successMessage').removeClass('d-none');

    // Clear the form
    this.reset();
  });
});
// affalite
$(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();

    // Show success message
    $('#successMessage').removeClass('d-none');

    // Reset the form
    this.reset();
  });
});
// marketing auto
$(document).ready(function () {
  $('#applicationForm').on('submit', function (e) {
    e.preventDefault();

    // Show success message
    $('#successMessage').removeClass('d-none');

    // Clear the form
    this.reset();
  });
});

 